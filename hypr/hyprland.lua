local basedir = "./hyprland/"

-- base
require(basedir .. "base.lua")
require(basedir .. "binds.lua")
require(basedir .. "monitors.lua")
require(basedir .. "looknfeel.lua")
require(basedir .. "ruls.lua")

